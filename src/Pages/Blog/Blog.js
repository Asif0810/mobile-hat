import React from 'react';

const Blog = () => {
    return (
        <div>
            <b>1. what are the different to ways to manage a state in react application ?</b>
            <p>The state of any application is represented by the user interface of the application. The state is mutable and whenever the user interacts with the application and changes its state</p>
            <b>2.how does prototypical inheritanc work</b>
            <p>Prototypical inheritance allows us to reuse the properties or methods from one JavaScript object to another through a reference pointer function. All JavaScript objects inherit properties and methods from a prototype: Date objects inherit from Date.prototype</p>
            <b>3.React vs angular vs vue?</b>
            <p>React is a UI library, Angular is a fully-fledged front-end framework, while Vue.js is a progressive framework.</p>
            <b>what is unit test in react ?</b>
            <p>Unit Testing in React Unit testing is a method of testing that tests the individual software unit in theprocess of isolation. Check the output of a function for any given input</p>
        </div>
    );
};

export default Blog;